import { styled } from "styled-components";

export const AddHouseStyle = styled.div``;

AddHouseStyle.Block = styled.section`
   width: 100%;
   padding: 24px 30px 48px;
   margin-bottom: 48px;

   border-radius: 3px;
   border: 1px solid var(--color-white-smoke, #e6e9ec);

   background: var(--color-white);
   box-shadow: 0px 10px 30px 0px rgba(13, 38, 59, 0.05);
`;
