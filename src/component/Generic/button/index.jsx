// import React, { Children } from "react";
// import { ButtonStyle } from "./style";

// export const Button = ({ children, type, width, icon, height }) => {
//    return (
//       <ButtonStyle icon={icon} height={height} width={width} type={type}>
//          {icon && <i className={`icon-${icon}`}></i>}
//          {children}
//       </ButtonStyle>
//    );
// };
// export default Button;